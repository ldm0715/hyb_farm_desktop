/// 圆角令牌。
library;

abstract final class FarmRadii {
  /// 控件（Tab 选中块、按钮、输入框）。
  static const double control = 10;

  /// 普通卡片（指标卡、地块卡、列表项）。
  static const double card = 14;

  /// 大容器 / 分组容器。
  static const double container = 16;

  /// 小圆角（作物图标、chip）。
  static const double small = 6;

  /// 二级分段控件（库存/收益排行切换）圆角。
  static const double segmented = 12;
}
